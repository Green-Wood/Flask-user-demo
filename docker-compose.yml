version: '3'
services:
  postgres:
    image: 'postgres:latest'

  api:
    build:
      dockerfile: Dockerfile
      context: ./backend

  nginx:
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
      - "80:80"