language: python

sudo: required

branches:
  only:
  - master

services:
  - docker

before_install:
  - docker-compose up --build -d

script:
  - docker run api python -m unittest

after_script:
  - docker-compose -f docker-compose.yml down